{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:charon:workload-claims:v1",
  "title": "Charon workload identity claims",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "iss",
    "aud",
    "sub",
    "tenant",
    "persona",
    "workspace",
    "lease",
    "operation",
    "capability",
    "jti",
    "iat",
    "nbf",
    "exp"
  ],
  "properties": {
    "iss": { "$ref": "#/$defs/issuerValue" },
    "aud": { "$ref": "#/$defs/issuerValue" },
    "sub": { "$ref": "#/$defs/auditIdentifier" },
    "tenant": { "$ref": "#/$defs/auditIdentifier" },
    "persona": { "$ref": "#/$defs/auditIdentifier" },
    "workspace": { "$ref": "#/$defs/auditIdentifier" },
    "lease": { "$ref": "#/$defs/auditIdentifier" },
    "operation": { "$ref": "#/$defs/auditIdentifier" },
    "capability": { "$ref": "#/$defs/auditIdentifier" },
    "jti": { "$ref": "#/$defs/nonce" },
    "iat": { "type": "integer", "minimum": 0 },
    "nbf": { "type": "integer", "minimum": 0 },
    "exp": { "type": "integer", "minimum": 1 }
  },
  "$defs": {
    "issuerValue": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "auditIdentifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._-]+$"
    },
    "nonce": {
      "type": "string",
      "minLength": 16,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._-]+$"
    }
  }
}
