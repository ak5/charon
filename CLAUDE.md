# Claude Guidance

Follow `AGENTS.md`. Charon handles credentials, so maintain deny-by-default
policy, redacted observability, exact destination matching, and fail-closed
behavior. Run `mise run check` before proposing changes.

